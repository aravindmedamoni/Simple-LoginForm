<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.medamoniaravind.loginform">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginFormActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterFormActivity"
            android:label="Registerform"/>
        <activity android:name=".ForgotPasswordActivity"
            android:label="ForgotPasswordForm"/>
        <activity android:name=".SetNewPassordActivity"
            android:label="NewPasswordForm"/>
        <activity android:name=".LocationActivity"
            android:label="Locations"></activity>
    </application>

</manifest>